# Themis 试跑包（v0.2 前）

这份试跑包的目的不是测单个函数，而是回答一个更实际的问题：

- 当前内核在真实工作流里最先卡在哪？
- 下一步该继续补 **工具化/数据入口**，还是转去做 **更强识别**？

建议先跑下面 7 组场景。

---

## 试跑 1：经典后门 + 数值闭环

文件：

- [numeric_backdoor.json](C:\Users\12916\Desktop\项目\因果性ai\tests\test_e2e\fixtures\numeric_backdoor.json)

你在看什么：

- 后门识别是否自然
- 公式是否可读
- 数值结果是否直观

预期：

- `effect(cancer=true | do(smokes=false))`
- `status = numerically_solved`
- 数值 `0.18`

---

## 试跑 2：缺参数 → 给 skeleton → 回填

文件：

- [numeric_backdoor_missing_parameter.json](C:\Users\12916\Desktop\项目\因果性ai\tests\test_e2e\fixtures\numeric_backdoor_missing_parameter.json)

你在看什么：

- `needs_investigation` 是否够诚实
- skeleton 是否已经足够形成闭环
- 缺口报告是否容易操作

预期：

- `status = needs_investigation`
- 缺失项精确指向一条 `ProbabilityKey`
- investigation request 带可粘贴 `probabilityStatement` 骨架

---

## 试跑 3：纯分布查询，不依赖因果图

文件：

- [probability_no_graph.json](C:\Users\12916\Desktop\项目\因果性ai\tests\test_e2e\fixtures\probability_no_graph.json)

你在看什么：

- `probability` 查询是否已经真正和 DAG 解耦
- 纯 Theta 程序是否能独立成立

预期：

- 空 DAG
- `probability(coin(a)=true)` 直接数值求值成功

---

## 试跑 4：多调整变量识别

文件：

- [identify_two_var.json](C:\Users\12916\Desktop\项目\因果性ai\tests\test_e2e\fixtures\identify_two_var.json)

你在看什么：

- 多变量后门识别是否还能解释清楚
- 解释层是否忠于公式 AST

预期：

- `status = structurally_solved`
- 公式里出现两个嵌套 `sum`
- 解释里必须同时说出两个调整变量

---

## 试跑 5：confidence 是否真的有用

文件：

- [numeric_backdoor_confidence_mixed.json](C:\Users\12916\Desktop\项目\因果性ai\tests\test_e2e\fixtures\numeric_backdoor_confidence_mixed.json)

你在看什么：

- `slice 9` 的 confidence 语义在真实输出里是否有辨识度
- `min` 规则会不会让你觉得“过保守”
- confidence 字段是否真的能帮助你决定下一步

场景：

- 和试跑 1 同一个后门图
- 四条 `probability` 语句都带 `annotations.confidence`
- 其中一条故意较弱：`0.3`

预期：

- `effect(cancer=true | do(smokes=false))`
- 仍然 `numerically_solved`
- 数值仍是 `0.18`
- `confidence = 0.3`

如果你看到这个结果时觉得：

- “对，这正是我想要的最弱链路含义”  
  那 slice 9 的方向就算站住了。

- “不，这太保守了，我其实想奖励多条独立证据”  
  那就说明 v0.3 的“独立证据声明 / opt-in 合成规则”是真需求。

---

## 试跑 6：教育场景，但变量仍然没被操作化

文件：

- [tutoring_exam_complete.json](C:\Users\12916\Desktop\项目\因果性ai\tests\test_e2e\fixtures\tutoring_exam_complete.json)

你在看什么：

- Themis 是否能顺利回答“补课会不会提高通过率”
- 即使图和参数都齐，变量 `pass_exam` 是否仍然显得太粗
- 这会不会再次暴露“变量框定不足”而不是“识别不够”

预期：

- `effect(pass_exam=true | do(tutoring=true))`
- `status = numerically_solved`
- 数值约为 `0.66`

观察点：

- 这个结果没有说明是哪门考试、哪个时间窗口、通过阈值是什么
- 如果你看到这里也会本能地追问“这到底是哪个考试、多久之后的结果”，说明命题充分性缺口不是 `exercise_waist` 的偶然问题

---

## 试跑 7：睡眠/专注场景，但时间尺度仍然缺失

文件：

- [sleep_focus_complete.json](C:\Users\12916\Desktop\项目\因果性ai\tests\test_e2e\fixtures\sleep_focus_complete.json)

你在看什么：

- Themis 是否能顺利回答“早点睡会不会更专注”
- 结果变量 `focus_better` 是否同样过于模糊
- “时间窗口没写清”会不会再次出现为第一痛点

预期：

- `effect(focus_better=true | do(sleep_early=true))`
- `status = numerically_solved`
- 数值约为 `0.54`

观察点：

- 这个结果没有说明是第二天、这一周、还是长期状态
- 如果这里你也会立刻追问“专注提高是怎么定义的、在哪个时间窗里”，说明 Themis 下一步真正缺的不是更多识别算法，而是变量/问题框定层

---

## 建议的观察维度

每跑完一个场景，都记一下这 5 件事：

1. 写输入费不费劲
2. 结果一眼能不能懂
3. 缺口能不能直接补
4. 解释和公式是否一致
5. 你最想继续补的是哪一层

---

## 推荐运行方式

直接用：

```powershell
python scripts/run_trial_pack.py
```

也可以只跑某一个：

```powershell
python scripts/run_trial_pack.py tests/test_e2e/fixtures/numeric_backdoor.json
```

这个 runner 会：

- 解析并校验输入
- 实例化、投影图、调度查询
- 输出每个 query 的状态、数值、confidence、解释
- 对 `needs_investigation` 额外列出 skeleton 摘要

---

## 如何用试跑结果决定下一步

- 如果你最常卡在“缺什么参数、怎么补、来源怎么记”，优先继续工具化/数据入口
- 如果你最常卡在“公式明明对了，但用户看不懂”，优先继续解释层
- 如果你最常卡在“confidence 给了，但还不足以支持决策”，再考虑 source 结构化或更强证据语义
- 如果你第一次真正遇到“后门不够、但按理论仍可识别”的场景，再启动 ADMG / ID 方向

# World Modeling

## 目的

这份文档定义的是 **Themis 之前的那一层**：

- Themis 负责：在**已知结构、已知变量、已知参数**上做形式推理
- World Modeling 负责：从世界事实、语料、记录和交互中，逐步构建出一个**足够可推理的候选世界模型**

如果不把这层单独讲清楚，系统就会永远停留在：

- 人手工命名变量
- 人手工声明关系
- Themis 只负责算

这不是终局。

---

## 核心判断

**自动世界建模，不是“从文本直接吐出最终因果图”。**

更现实也更正确的目标是：

**从事实语料中构建候选变量、候选事实、候选关系，再经过约束、澄清、验证和筛选，收敛成一个可交给 Themis 的世界模型。**

所以它本质上是一个：

- 候选生成
- 规范化
- 关系筛选
- 模型收敛
- 反馈修正

的过程，而不是一步到位的“真图发现器”。

---

## 不是什么

World Modeling 不是：

- 直接从自然语言生成最终因果 DAG
- 只靠 LLM 猜关系
- 只靠统计相关就宣布因果
- 替代 Themis 的推理层

它更像是：

**Themis 的上游模型构建器**

负责把“世界里散乱的事实和描述”压成“可以正式推理的候选结构”。

---

## 总体链路

长期系统的核心链路应当是：

```text
事实语料 / 记录 / 交互输入
    -> 变量构建
    -> 事实抽取
    -> 候选关系生成
    -> 模型收敛
    -> 编译到 Themis
    -> 推理 / 缺口 / 调查 / 反馈
```

也就是说，Themis 不是系统的入口，而是中下游的**形式推理核心**。

---

## 五步法

## 1. 变量构建

第一步不是先抽边，而是先回答：

- 世界里有哪些对象？
- 有哪些状态变量？
- 有哪些事件变量？
- 每个变量的时间窗口是什么？
- 阈值和测量方式是什么？

例如用户说：

- “我想开始运动减少肚子上的肉”

这不能直接进入 Themis。  
需要先构造成更可推理的变量，例如：

- `exercise_regular_12w(me)`
- `diet_control_12w(me)`
- `waist_down_3cm_12w(me)`

这里最重要的不是命名本身，而是：

- 时间窗口
- 结果阈值
- 测量方式
- 值域

这一步本质上是在做：

**自然语言目标 -> 可操作化变量**

---

## 2. 事实抽取

在变量被定义后，系统才能从语料里抽事实。

例如：

- `observation(exercise_regular_12w(me)=true)`
- `observation(diet_control_12w(me)=false)`
- `observation(waist_down_3cm_12w(me)=false)`

事实来源可以是：

- 文本
- 表格
- 记录
- 用户问答
- 传感器
- 历史日志

这一步的目标不是抽关系，而是建立：

**谁在什么时候、在什么定义下，取了什么值**

---

## 3. 候选关系生成

这一步才开始生成边，但要明确：

**生成的是候选关系，不是最终真理。**

候选关系通常有三类来源：

### 3.1 语言/知识来源

来自：

- 规则库
- 教科书
- 领域知识
- LLM 对文本的结构化解释

例如：

- `diet_control -> waist_down_3cm_12w`
- `exercise_regular_12w -> waist_down_3cm_12w`

### 3.2 数据来源

来自：

- 观测数据
- 条件独立结构
- 统计相关
- 因果发现算法的输出

它们通常不能直接证明因果，但可以形成候选边或反对某些边。

### 3.3 结构先验

来自：

- 时间顺序
- 领域限制
- 不允许的边类型
- 层级约束

例如：

- 未来不能指向过去
- 测量变量不应反向导致其定义对象

---

## 4. 模型收敛

候选边必须经过收敛过程，才能进入正式模型。

这一步通常包括：

- 同义变量合并
- 冲突边处理
- 时间一致性检查
- 值域一致性检查
- 人工确认
- 反例驱动修正

这里最关键的原则是：

**系统必须允许“不确定模型”存在。**

所以边最好至少有这些状态：

- `proposed`
- `accepted`
- `rejected`
- `unknown`

只有 `accepted` 的结构，才应该进入 Themis 的正式因果推理。

---

## 5. 编译到 Themis

当模型收敛到一定程度后，再编译成 Themis 可消费的对象：

- `cause(...)`
- `probability(...)`
- `observation(...)`
- `query(...)`

换句话说：

**Themis 不是用来“建模”的，而是用来“在已接收模型上推理”的。**

---

## 核心对象

如果要把 World Modeling 做成正式层，我建议至少定义下面这几类对象。

## 1. VariableSpec

描述一个变量本身。

典型字段：

- `predicate`
- `domain`
- `time_window`
- `measurement`
- `threshold`
- `unit`
- `observability`
- `aliases`

## 2. FactItem

描述一条事实或观测。

典型字段：

- `variable`
- `value`
- `subject`
- `time`
- `source`
- `confidence`

## 3. RelationHypothesis

描述一条候选关系。

典型字段：

- `from`
- `to`
- `relation_type`
  - `association`
  - `causal_hypothesis`
  - `constraint`
- `status`
  - `proposed`
  - `accepted`
  - `rejected`
  - `unknown`
- `support`
- `confidence`
- `source_refs`

## 4. CandidateWorldModel

把变量、事实、候选边、冲突和来源组织在一起。

它是 Themis 的直接上游对象。

---

## 与 Themis 的边界

最重要的一条边界是：

**World Modeling 负责“模型从哪里来”，Themis 负责“模型给定后怎么推”。**

所以：

- 变量没框清 -> World Modeling / framing gate 的问题
- 候选边冲突 -> World Modeling 的问题
- 已接受结构上 effect 怎么算 -> Themis 的问题
- 参数缺什么、下一步补什么 -> Themis 的问题

这条边界一定要保持清楚，不然系统会重新混成一层。

---

## 为什么现在要关心它

因为你已经反复碰到一个问题：

- 系统能算
- 但问题本身还没被定义清楚

这说明当前真正缺的，不只是推理小功能，而是：

**在进入 Themis 之前，谁来负责把世界问题压成一个可推理问题。**

答案不是“让 Themis 自己猜”，而是：

**补一个上游的 World Modeling / Framing 层。**

---

## 推荐路线

如果以后真要做 World Modeling，我建议不是直接做“自动因果图生成”，而是按下面顺序推进：

1. **变量框定器**
   - 把模糊问题转成可操作化变量

2. **事实抽取器**
   - 把观测值、记录、文本证据落到变量上

3. **候选关系生成器**
   - 从文本、知识库、数据里提出边

4. **模型收敛器**
   - 处理冲突、筛选、确认、约束

5. **Themis 编译器**
   - 把 accepted 的结构编译给推理内核

也就是说：

**第一步不是“抽因果边”，而是“先把变量定义对”。**

---

## 当前结论

对当前阶段来说，最重要的不是再扩 Themis 的小语义，而是始终记住：

- Themis 当前是推理层
- World Modeling 才是长期入口层

只有把这两层分开，系统才不会一边要求形式语义，一边又偷偷让推理层承担建模层的工作。

---

## 一句话

**World Modeling 的任务，是把世界事实逐步压成一个足够清楚、足够一致、足够可推理的候选世界模型，再把它交给 Themis 做正式推理。**

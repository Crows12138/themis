# 因果推理内核理论框架 v0.1

这份文档只做一件事：

给当前项目建立一套正式的理论基线。

它不讨论代码接口，不讨论 UI，不讨论工程分层，只回答下面几个理论问题：

1. 语言里的对象到底是什么
2. 一个“模型”到底由什么组成
3. 因果、相关、观测、干预在语义上分别是什么意思
4. 每类查询什么时候算有定义、什么时候算可解
5. 我们追求的完备性到底是相对于什么

---

## 1. 总体立场

当前系统采用下面这组理论立场：

- 世界在当前模型内被表示为一个有限对象域上的关系化结构
- 因果结构是确定的、显式给出的、封闭的
- 数值层是不确定的，允许缺失、置信度和调查需求
- 图不是本体，而是关系表达在当前对象域上的工作投影
- 因果性由有向因果链定义
- 实际相关性由当前条件下的开放路径定义
- 新证据主要通过概率更新进入系统
- 结构推理是单调的，不支持默认规则撤回
- v0.1 只处理静态世界，不处理时间、动作序列和反事实

这意味着：

- 结构不确定性暂时不进入核心语义
- 现实中的不确定主要放在参数、样本、观测噪声和置信度上

---

## 2. 对象宇宙

### 2.1 对象域

设当前对象域为一个有限集合：

```text
D = {d1, d2, ..., dn}
```

其中每个元素表示当前模型中被显式纳入的对象，例如：

- `alice`
- `bob`
- `drug_1`
- `city_A`

v0.1 的理论默认：

- 只在 `D` 上量化和推理
- 不直接对无限对象域做语义解释
- 不自动引入未实例化的新对象

### 2.2 谓词与原子

设有一组谓词符号：

```text
P = {p1, p2, ..., pk}
```

一个原子形如：

```text
p(t1, t2, ..., tm)
```

其中每个 `ti` 是对象常量或变量。

对当前对象域实例化后，得到 ground atom，例如：

```text
smokes(alice)
tar(alice)
cancer(alice)
```

### 2.3 关系表达不是图本身

理论上，本体层是：

- 对象
- 谓词
- 关系
- 因果声明
- 概率声明

图只是把这些关系在当前对象域上展开之后得到的一个工作结构。

---

## 3. 语言层的形式对象

我们把程序中的核心形式对象抽象为：

- 3 类模型对象：`cause`、`probability`、`observation`
- 1 类查询内嵌操作：`do`
- 1 类查询对象：`query`

### 3.1 因果声明

```text
cause(A, B)
```

其中 `A`、`B` 可以是模式原子，也可以是实例原子。

语义：

- 表示 `A` 对 `B` 存在直接结构因果依赖

### 3.2 概率声明

```text
prob(T | G) = v
```

其中：

- `T` 是目标原子
- `G` 是条件原子集合
- `v ∈ [0,1]`

语义：

- 表示在当前模型世界中，一个条件概率项被显式给定
- 它属于模型分布，而不是样本统计摘要

补充规定：

- `G` 被解释为 `T` 的模型条件集合
- 在 v0.1 中，`G` 语义上应与 `T` 的结构父集一致，或是其子集
- 若需要表达经验频率、样本估计或边际化后的观测统计，应在未来使用单独的经验概率语句，而不是复用 `prob`

### 3.3 观测声明

```text
observe(A = value)
```

语义：

- 表示当前世界中对某一实例变量有观测证据
- 观测证据进入证据集，而不是进入世界本体
- 高置信观测可以在当前轮推理中作为硬证据使用，但语义上仍属于证据，不是独立的世界事实

因此：

- v0.1 不设独立 `fact` 语义
- 所谓“事实输入”应被理解为 `confidence = 1` 的观测特例

### 3.4 查询内嵌的干预操作

```text
do(A = value)
```

语义：

- 不是事实
- 不是普通观测
- 不是可独立存在的模型声明
- 而是查询内部对模型生成机制施加的外部操作

### 3.5 查询

查询是对模型提出的问题，而不是模型的一部分。

例如：

- `cause(X, Y)`
- `assoc(X, Y | C)`
- `effect(Y | do(X=x), C)`
- `prob(Y | X)`
- `identify(prob(Y | do(X=x), C))`

---

## 4. 模型定义

当前项目里的一个理论模型记作：

```text
M = (D, V, E, Theta, O, Gamma)
```

各项含义如下。

### 4.1 `D`：对象域

当前纳入推理的有限对象集合。

### 4.2 `V`：实例变量集合

`V` 是在对象域 `D` 上实例化之后得到的全部工作变量。

例如若有规则：

```text
cause(smokes(X), tar(X))
```

且 `D = {alice, bob}`，则 `V` 中包含：

- `smokes(alice)`
- `tar(alice)`
- `smokes(bob)`
- `tar(bob)`

### 4.3 `E`：因果边集合

`E ⊆ V × V`

若 `(A, B) ∈ E`，表示存在直接因果边：

```text
A -> B
```

### 4.4 `Theta`：参数层

`Theta` 包含所有数值参数，例如：

- 条件概率项
- 先验
- 参数来源
- 参数可信度

注意：

- `Theta` 中的 `prob` 项表示模型分布
- 经验样本统计不默认属于 `Theta`，除非未来被专门纳入

### 4.5 `O`：观测集

`O` 是当前被采纳的观测证据集合。

### 4.6 `Gamma`：置信度与元信息

`Gamma` 表示对观测和参数附带的可信度、来源、样本质量等元信息。

其中：

- `confidence` 被解释为复合可信度分数
- 它不是统计置信区间
- 它也不自动等于贝叶斯后验概率
- 它表示系统当前对某条输入项整体可靠性的综合评价

这意味着当前模型不是纯布尔模型，而是：

- 结构层确定
- 参数层带不确定和可信度

---

## 5. 编译语义

关系表达本体需要被编译为工作模型。

### 5.1 实例化

对每个带变量的关系声明，在当前对象域 `D` 上做有限实例化。

例如：

```text
forall X:
  cause(smokes(X), tar(X))
```

若 `D = {alice, bob}`，则得到：

```text
cause(smokes(alice), tar(alice))
cause(smokes(bob), tar(bob))
```

### 5.2 图投影

所有实例化后的 `cause(A, B)` 声明共同生成有向图：

```text
G(M) = (V, E)
```

这张图是：

- 路径分析的基础
- 相关判断的基础
- 因果查询的基础

### 5.3 保真要求

编译过程必须满足：

- 每个逻辑实例都能映射为明确工作节点
- 每个结构因果声明都能映射为明确工作边
- 图上的任一结论都能回溯到原始关系表达

这条要求保证：

- 图不是语义黑箱
- 图只是关系表达的可计算展开

---

## 6. 因果语义

### 6.1 直接因果

若 `(A, B) ∈ E`，则称：

```text
A` 直接因果影响 `B`
```

### 6.2 间接因果

若在 `G(M)` 中存在一条从 `A` 到 `B` 的有向路径：

```text
A -> ... -> B
```

则称：

```text
A` 结构上因果影响 `B`
```

### 6.3 因果性的形式定义

定义：

```text
Cause_M(A, B) = true
iff
在 G(M) 中存在从 A 到 B 的有向路径
```

否则：

```text
Cause_M(A, B) = false
```

### 6.4 诊断不改变因果方向

即使：

```text
Cause_M(A, B) = true
```

并且可以从 `B` 的观测更新对 `A` 的相信程度，

也不意味着：

```text
Cause_M(B, A) = true
```

因此：

- 诊断推理是概率更新
- 不是因果方向反转

---

## 7. 相关语义

### 7.1 相关不是只看因果链

当前项目定义的“实际相关性”是：

- 在当前条件集下，两个变量之间是否仍存在开放路径

而不是：

- 它们之间是否有直接因果边

### 7.2 路径类型

路径上的局部结构只有三种基本情况：

- 链：`X -> M -> Y`
- 叉：`X <- Z -> Y`
- 碰撞点：`X -> C <- Y`

### 7.3 开放路径规则

对任意路径 `pi`，给定条件集 `C`：

- 普通中间点若属于链或叉，被条件化则堵路
- 碰撞点默认堵路，被条件化则开路

### 7.4 实际相关性的形式定义

定义：

```text
Assoc_M(A, B | C) = true
iff
在 G(M) 中存在至少一条从 A 到 B 的开放路径，且开放性相对于条件集 C 判断
```

否则：

```text
Assoc_M(A, B | C) = false
```

### 7.5 相关来源

`Assoc_M(A, B | C)` 可能来自：

- 因果链
- 共同原因路径
- 被条件化打开的碰撞路径

因此：

- 后门只是相关来源中的一种
- 不是相关性的全部定义

---

## 8. 观测语义

### 8.1 观测是条件化

`observe(A = v)` 的语义不是“改变世界”，而是：

- 把当前推理放在 `A = v` 这个条件下进行

所以观测主要影响：

- 后验概率
- 路径开放状态
- 相关判断

### 8.2 观测不改结构

普通观测不会改变：

- `D`
- `V`
- `E`

只会改变：

- `O`
- 基于 `O` 的概率更新结果

---

## 9. 干预语义

### 9.1 干预不是观测

`do(A = v)` 的语义是：

- 外部强制把变量 `A` 设为值 `v`
- 而不是单纯看到 `A = v`

### 9.2 干预模型

定义干预后的模型：

```text
M_do(A=v)
```

它满足：

- 删除所有指向 `A` 的入边
- 把 `A` 固定为 `v`
- 其余结构保持不变

### 9.3 因果效应查询

因果效应查询的目标是：

```text
P_M(Y | do(X=x))
```

它不是：

```text
P_M(Y | X=x)
```

这两者在语义上必须严格区分。

---

## 10. 概率语义

### 10.1 概率层角色

概率层不决定因果结构本身，而负责：

- 观测更新
- 数值估计
- 置信度表达

### 10.2 条件概率项

如果 `Theta` 中给定：

```text
prob(T | G) = v
```

则表示一个模型参数项被输入模型。

这里的 `G` 不是任意观察条件，而是模型语义中的条件集合。

因此：

- `prob(cancer(X) | tar(X))` 可以是结构参数
- `prob(cancer(X) | smokes(X))` 若不对应目标父集，则不应在 v0.1 中直接作为模型参数写入
- 若用户想表达观测统计，应在未来使用独立的经验概率语句

此约束在 v0.1 以 **graph-level 语义检查 `probability_parents`** 强制：`G ⊆ parents(T)` 在 `G(M)` 中必须成立，否则 `validate_against_graph` 抛出语义错误，Theta 连建都不建。检查在 `dispatch_all` 内部隐式执行。

### 10.4 数值估计

v0.1 的数值层已落地，包含：

- `Theta`：参数存储，键是 `(target_atom, target_value, frozenset(given))`；值域由 `theta_builder` 从出现过的值自动推断（未出现的原子退回布尔默认）
- `numeric_estimator.estimate_formula(formula, theta)`：递归求值 `constant / probability_ref / product / sum`；sum 在 `theta.domain_of(over)` 上遍历并代入 `VarRef`；缺参抛 `InsufficientTheta`，携带精确 `ProbabilityKey`
- `scheduler` 对 `effect` / `probability` 查询试求值；成功则 `NUMERICALLY_SOLVED`，失败则 `NEEDS_INVESTIGATION` 且 `missing_information` 指出具体哪条条件概率缺失

注意：`confidence_calc.composite` 仍是 v0.1 占位（min 规则），形式语义留给 v0.2。

### 10.3 概率更新

新证据到来时，系统优先做：

- 条件更新
- 后验更新
- 参数修正

而不是把“通常如此”先当成布尔真，再在逻辑层撤回。

这意味着 v0.1 的信念修正机制主要是：

- 概率式
- 证据驱动

---

## 11. 查询语义

理论上，查询必须先被分型。

因为“可解”永远是相对于查询类型定义的。

### 11.1 因果存在性查询

形式：

```text
cause(A, B)
```

语义：

- 问 `Cause_M(A, B)` 是否成立

可解条件：

- 结构给定即可

### 11.2 相关查询

形式：

```text
assoc(A, B | C)
```

语义：

- 问 `Assoc_M(A, B | C)` 是否成立

可解条件：

- 结构给定即可

### 11.3 干预效应查询

形式：

```text
effect(Y | do(X=x), C)
```

语义：

- 问干预模型 `M_do(X=x)` 下，在条件集 `C` 已知时，`Y` 的行为或分布

可解条件：

- 结构语义必须定义
- 数值层若不足，则只能部分回答

### 11.4 识别查询

形式：

```text
identify(prob(Y | do(X=x), C))
```

语义：

- 问该干预量是否能由当前模型中可用的观测量表达

若可识别，则理论上应返回一个结构化公式表达，而不是仅供显示的自然语言或字符串

### 11.5 数值查询

形式：

```text
prob(Y | X)
prob(Y | do(X=x), C)
```

语义：

- 问具体数值，而不是只问结构真假

### 11.6 公式子语言

当 `identify` 查询成功时，结果中的 `formula` 字段用一门独立的公式子语言表达。

该子语言的正式规范见 `formula_ast_spec_v0_1.md`。

理论层要点：

- 公式 AST 只含四个算子：`constant`、`probability_ref`、`product`、`sum`
- `sum` 显式绑定一个值变量，在 ground atom 的取值域上求和
- 公式必须闭合，不允许自由值变量；查询上下文中的原子不参与自由性判定
- `probability_ref` 指向模型分布中的任意条件概率项，不限于 `Theta` 的字面条目
- 识别失败时不返回公式，应通过 `status` 或 `missing_information` 表达
- 公式层中永远不出现 `do` 节点；识别的全部语义目的就是消除 `do`

---

## 12. 可解性定义

当前项目把“可解”拆成三层。

### 12.1 结构可解

若某查询只依赖 `G(M)` 就能决定，则称为结构可解。

例如：

- `cause(A, B)`
- `assoc(A, B | C)`

### 12.2 数值可解

若某查询在结构定义成立后，`Theta` 和 `O` 也足够支持数值计算，则称为数值可解。

例如：

- `prob(Y | X)`
- `prob(Y | do(X=x), C)`

### 12.3 调查可推进

若某查询：

- 结构语义已定义
- 查询本身属于语言范围
- 但数值数据不足

则称其为调查可推进。

此时系统不应回答“无意义”或“失败”，而应返回：

- 结构上已知什么
- 公式上缺什么
- 调查上该补什么

### 12.4 语言外

若某查询超出当前语言定义范围，则为语言外问题。

例如：

- 时间规划
- 反事实
- 高阶元推理

---

## 13. 输出语义

每个语言内查询都必须落入下面四类状态之一：

- `structurally_solved`
- `numerically_solved`
- `needs_investigation`
- `outside_language`

这些不是工程字段而已，而是理论分层。

它们对应：

- 结构已解
- 数值已解
- 可继续调查
- 当前版本无语义定义

补充约束：

- 若输出包含 `formula`，它应是可继续估计和化简的结构化表达
- 若输出包含 `confidence`，它表示对整个结果的复合可信度，而不是统计置信区间

---

## 14. 完备性目标

这里的“完备”不是抽象地“什么都能算”，而是：

**相对于当前语义定义，所有本应能推出的结论都能推出。**

所以完备性必须按查询类型来定义。

### 14.1 因果存在性完备

若对任意 `A, B ∈ V`：

- 只要 `Cause_M(A, B) = true`，系统都能发现至少一条有向路径
- 只要 `Cause_M(A, B) = false`，系统都不会错误报告因果链

则称因果存在性求解器相对于 `Cause_M` 完备且可靠。

### 14.2 相关判断完备

若对任意 `A, B ∈ V` 和条件集 `C`：

- 只要 `Assoc_M(A, B | C) = true`，系统都能发现至少一条开放路径
- 只要 `Assoc_M(A, B | C) = false`，系统都不会错误报告开放路径

则称相关判断求解器相对于 `Assoc_M` 完备且可靠。

### 14.3 数值层完备

数值层的完备性更强，也更难。

它要求：

- 只要当前模型和参数足以确定目标数值，系统就能算出
- 若数值尚不确定，系统能准确指出缺失条件

v0.1 暂不假设数值层已完全完备，但要求：

- 结构层完备优先成立
- 数值层至少能区分“已解”和“待调查”

---

## 15. 核心推理规则

本节给出当前理论允许的最小推理规则集合。

### 15.1 实例化规则

若：

```text
forall X: cause(P(X), Q(X))
```

且 `a ∈ D`，则可推出：

```text
cause(P(a), Q(a))
```

### 15.2 图投影规则

若：

```text
cause(A, B)
```

则在工作图中加入边：

```text
A -> B
```

### 15.3 有向链规则

若在工作图中存在：

```text
A -> ... -> B
```

则：

```text
Cause_M(A, B) = true
```

### 15.4 开放路径规则

若在 `A` 与 `B` 之间存在一条路径，且相对于条件集 `C`：

- 所有普通中间点不被条件化
- 所有碰撞点满足开放条件

则该路径是开放路径。

### 15.5 相关规则

若存在至少一条 `A` 到 `B` 的开放路径，则：

```text
Assoc_M(A, B | C) = true
```

否则：

```text
Assoc_M(A, B | C) = false
```

### 15.6 干预规则

若查询中出现：

```text
do(A=v)
```

则构造 `M_do(A=v)`，在该模型上评价干预查询。

### 15.7 调查规则

若查询在结构上已定义，但数值上缺关键参数，则：

- 不返回“假”
- 不返回“无意义”
- 返回 `needs_investigation`

并附带缺失条件和调查需求

---

## 16. 边界条件

当前理论框架刻意不处理：

- 时间结构
- 多步动作
- 规划问题
- 反事实变量
- 高阶逻辑上的规则量化
- 无限对象域上的直接语义

这些不是“不重要”，而是：

- 它们会引入新的语义层
- 应在后续版本单独定义

---

## 17. 最小理论闭环

如果只保留最短闭环，可以压成下面这组：

1. 对象域是有限的
2. 关系表达在对象域上实例化
3. 因果声明编译成工作图
4. 因果性由有向链定义
5. 相关性由开放路径定义
6. 观测只做条件化
7. 干预通过切断入边并固定变量定义
8. 结构可解、数值可解、调查可推进必须分开
9. 完备性相对于给定语义定义，而不是抽象地“什么都能算”

---

## 18. 一句总纲

```text
在一个有限对象域上的关系化模型中，把因果声明编译为图，把因果定义为有向链，把相关定义为开放路径，把观测定义为条件化，把干预定义为结构手术，并把可解性分为结构、数值和调查三层。
```

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/

# Virtual environments
.venv/
venv/
env/
ENV/

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# Type checkers
.mypy_cache/
.pyre/
.pytype/
.ruff_cache/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# MCP / language-server tooling
.serena/
.claude/
*.log

# OS
.DS_Store
Thumbs.db

# Local env / secrets (guard, not used yet)
.env
.env.local
*.key
*.pem

# Downloaded reference PDFs (freely available; cited by URL in tests) — not committed
references/

# The architecture picture, built by scripts/build_arch_spec.py plus an
# external renderer. Deliberately not committed: a drawing checked in
# here would be right on the day it was built and silently wrong after,
# which is the failure #619 exists to stop. The generator is the artifact
# that stays true; run it when you want a current picture.
docs/架构图/

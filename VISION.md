# Themis Vision

## 最终目标

Themis 的最终目标不是做一个只会回答几类因果查询的工具，而是做一个**有明确形式语义的 AI 推理内核**。

这个内核要满足三件事：

1. **表达清楚**
   不依赖自然语言的多义性；世界知识、因果关系、概率条件、观测和干预都要有明确表示。

2. **推理清楚**
   系统必须能区分：
   - 因果影响
   - 观测相关
   - 干预效应
   - 缺失信息

3. **推进问题**
   当信息不够时，不只是返回“不会”，还要指出：
   - 缺什么
   - 为什么缺
   - 下一步怎么补

这里的“严格”需要明确成下面这个含义：

- 不是“系统直接从混乱现实自动推出绝对真理”
- 而是“在显式建模假设、变量定义、结构声明和数据来源给定后，系统能够对相关性与因果结论给出**机器可验证的推导链**”

换句话说，Themis 长期追求的不是“自动宣布世界真相”，而是：

**构建一个能对相关性与因果结论做严格推导、并同时显式记录其前提、来源和未解决缺口的推理系统。**

从长期看，这个内核不应只消费“已经手工写好的关系图”，还应服务于一条更完整的链路：

- **事实语料**
- **关系/变量构建**
- **候选世界模型**
- **形式推理**
- **缺口推进与行动**

也就是说，Themis 的终局不是“手工建模后回答查询”，而是成为更大系统中的**推理层**：上游从事实语料中构建候选关系图，下游在其上做因果、相关、干预和缺口推进。

---

## 当前定位

当前的 Themis 还不是完整 AI 内核，而是它的**静态因果推理核心**。

它当前默认两件事已经由外部完成：

- 变量已经被命名并操作化定义
- 关系结构已经被声明进模型

也就是说，现在的 Themis 负责“**已知模型时如何推理**”，还不负责“**从事实语料自动构建模型**”。

现在已经具备：

- 关系级有限对象域表达
- 因果 DAG 投影
- cause / assoc / identify / effect / probability 五类查询
- 后门识别
- 数值求值 MVP
- confidence 聚合
- 缺参数调查与 skeleton 回填闭环
- 中文解释层

也就是说，当前系统已经能处理：

- “有没有因果影响”
- “为什么相关”
- “这个干预量能不能识别”
- “如果参数足够，数值是多少”
- “如果参数不够，下一步该补什么”

但它目前不会自动回答：

- 这个变量定义得是否足够清楚
- 这个关系是否应该从语料中抽出来
- 这条边是事实、相关还是因果假设

---

## 不是什么

Themis 当前**不是**：

- 通用聊天模型
- 纯逻辑证明器
- 仅做知识图谱查询的系统
- 完备因果识别器
- 带潜变量 / ADMG / 反事实 / 时间动作语义的全因果平台

这些都可能是未来方向，但不是当前版本的身份。

---

## 中期目标

中期目标是把 Themis 做成一个**稳定、可扩展、可审计的静态因果推理系统**，并让它在真实工作流里可用。

这意味着：

- 表达层继续收敛
- 结果解释继续对齐公式和数值
- 参数补录、调查推进、证据追踪继续完善
- 用真实案例决定是否需要更强识别能力

同时，把 Themis 与未来的“世界建模层”边界划清：

- **世界建模层**：从事实语料中抽实体、事件、变量、候选关系
- **Themis 推理层**：在已接受的结构上做形式推理

这两层可以强耦合协作，但不应在概念上混成一层。

关于这层的正式定义，见
[WORLD_MODELING.md](C:\Users\12916\Desktop\项目\因果性ai\WORLD_MODELING.md)。

---

## 分层图景

长期系统更接近下面这个结构：

1. **语料层**
   文本、记录、表格、用户输入、传感器数据

2. **构建层**
   实体抽取、变量规范化、关系抽取、候选因果/相关边生成

3. **推理层（Themis）**
   因果识别、相关判断、干预查询、数值求值、缺口推进

4. **行动层**
   调查、补参数、重跑、解释、决策支持

当前 Themis 主要处于第 3 层，并与第 4 层开始形成闭环。

---

## 长期跃迁

如果未来出现下面这些需求或明确的理论目标：

- 后门不够
- 需要潜变量
- 需要双向边 / ADMG
- 需要前门 / ID
- 需要反事实
- 需要时间和动作

那 Themis 会从“静态因果核心”跃迁成更完整的推理内核。

这里需要区分两件事：

- **当前承诺范围内的理论闭合**，应该优先完成，不需要每次都由新案例来证明必要性
- **超出当前范围的语义跃迁**，可以由真实需求触发，也可以由明确的理论立项目标触发

关键不是“能不能 theory-first”，而是：

**一旦决定跃迁，就必须把它显式当成一个新的 fragment / phase 来设计，而不是以补丁的方式偷偷长进现有内核。**

---

## 扩展愿景：LLM-native 全板块因果推理编排器（2026-04-24 起）

经过 Phase 0–5 的验证，Themis 的定位从"静态因果推理内核"**扩展**为一个
更大的目标：

**成为 LLM-native 因果定量推理的统一编排层——覆盖因果定量问题的全部
12 个板块，全部自家实现，全流程可审计。**

### 为什么扩展

- Phase 0-5 证明了 "JSON-in / JSON-out + 独立 verifier + LLM 前端"
  这套机制本身是有价值的架构——不只是为 Pearl 识别服务
- 真实 LLM 应用需要的不只是"backdoor 识别"，而是**整个因果定量问题
  空间**（IV / 中介 / 反事实 / 发现 / 敏感性 / 估计等）的统一接口
- 现有开源因果库（DoWhy / EconML / pgmpy / causal-learn）各自覆盖一
  个子集，没有任何一个做了"LLM-native + 审计完整 + 跨子领域统一"这
  件事——**这个位置目前真的空着**

### 覆盖目标：12 板块

Themis 最终目标覆盖因果定量问题的全部 12 板块，具体现状和策略见
[COVERAGE_MAP.md](COVERAGE_MAP.md)：

1. 可观测识别（Pearl backdoor / front-door / 完整 ID）
2. 潜变量 / ADMG
3. 反事实（Layer 3）
4. 时序 / 动态
5. 工具变量（IV）
6. 中介分析（NDE / NIE / CDE）
7. 选择偏差
8. 测量误差
9. 转移性 / 泛化
10. 敏感性分析
11. 连续变量 / 数据驱动估计
12. 因果发现（从数据反推 DAG）

### 执行原则

对外部开源生态的定位变化：

- **所有核心算法自家实现**——production 依赖里**不放任何因果推理库**
  （DoWhy / EconML / CausalML / pgmpy / causal-learn 都不放）
- 只依赖 ML 原语（`numpy / scipy / scikit-learn / networkx / pandas /
  pytorch`），这些库稳定、确定、可 pin
- **外部因果库降级为 parity calibration tool**——只在开发时跑对照测
  试确认我们写对了，production 路径不依赖它们
- API 调用的**5 条规则**（见 `COVERAGE_MAP.md`）：必须全部满足才允许
  接 API，否则自写。其中最核心的是"**估计层结果禁止走 API**"——因为
  默认参数漂移会破坏可审计承诺

### 里程碑

- **M1（≈ Phase 6）**：识别层完整化——IV / 中介 / 完整 ID
- **M2（≈ Phase 7）**：基础估计器——IPTW / g-computation / DML / DR-Learner
- **M3（≈ Phase 8）**：发现 + 敏感性——PC / FCI / E-value / Rosenbaum

三个里程碑共约 3.5-4 个月，每个都独立可展示。超出 M1-M3 的板块（测量
误差、转移性、L3b 深度方法等）按真实压力逐个立项。

### 和原 VISION 的关系

**不替换，只扩展**。原 VISION 对"静态因果推理核心"的描述仍然准确——
Themis 在 Phase 0-5 完成的就是那部分。扩展愿景把目标从"静态因果核心"
扩大到"全板块编排器 + LLM-native 前端"，把原 VISION 作为子集包住。

核心原则不变：显式建模假设、机器可验证推导链、缺口显式报告、每个数
字可追溯。这些在扩展到 12 板块后**更重要**，不是被放弃。

---

## 当前原则

当前阶段，Themis 优先坚持这四条原则：

1. **语义干净胜过功能堆积**
2. **缺口显式胜过伪智能补全**
3. **工作流闭环胜过孤立算法炫技**
4. **当前范围内优先追求理论闭合；超出当前范围的跃迁必须显式立项**

---

## 一句话

Themis 要成为的，是一个**能在上游世界模型之上清楚区分因果与相关、在信息不完整时继续推进问题、并最终服务于更大 AI 系统的推理内核**。

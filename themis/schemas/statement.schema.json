{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.local/causal-kernel/statement.schema.json",
  "title": "Statement v0.1",
  "description": "A sentence and this occasion's facts for its holes, as any artifact carries it. The shape `themis.language.state` writes and `themis.language.spoke` reads.\n\nIt lived in query_result's own `$defs` and was reachable only from there, which is the whole of why the six STANDALONE artifacts did not have it: each held its reader's text in a `note` or a `prompt` typed `string`, so a producer owing a reader a sentence had nowhere to put a second language and wrote one language at the site. That is not a missing translation, it is a missing slot — the finding `themis/estimation/discovery.py` records for its own six — and one cut across that channel is what the language debt table has been saying is owed. This file is that cut's door, and query_result now refers to it rather than restating it: one shape, one record.\n\nThe envelope carries no language. What crosses is which set, which member, and the occasion's values.",
  "$defs": {
    "said": {
      "x-text": "value",
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "Each of the species' sentence slots whose rendering is the same in every language, already rendered: a count, a column name, a stratum as `col=level`, a float at six significant figures, a collection sampled as `[a, b, ... +N]`. Brackets and quotation marks and the sampling cutoff are symbols, and symbols are what a sentence in any language puts around a value — so this half of the occasion can leave the kernel finished. It travels rendered rather than as the rule that renders it because a surface that cannot run that rule (the browser) would otherwise carry a second implementation of it, in a language nothing on this side can test. Together with `words` these are exactly the sentence's holes. Written once and pointed at from every channel that carries an occasion — a refusal, a missing item, an investigation item, a request's note, a question an orientation session puts to a person — because they are the same two halves and a second copy of the rule is a second thing to keep true."
    },
    "words": {
      "x-text": "identifier",
      "type": "object",
      "additionalProperties": {
        "oneOf": [
          { "$ref": "#/$defs/statement" },
          { "type": "array", "items": { "$ref": "#/$defs/statement" } }
        ]
      },
      "description": "The species' sentence slots whose rendering is NOT the same in every language: each is a member of a closed vocabulary whose text IS the language. They travel as the set and the token, and the surface that knows who is reading looks the word up in the table it already holds. Absent from most occasions — across both registries only a handful of slot names are ever filled this way. A slot may also hold a whole STATEMENT, which is the same shape with facts of its own and is assembled one level down, or a LIST of them, which the reader's surface joins in that language's punctuation. Without that, a sentence inside a sentence had to be assembled where it was built, which is the kernel picking a language, and in one place an ASCII comma between Chinese items."
    },
    "statement": {
      "type": "object",
      "additionalProperties": false,
      "required": ["vocabulary", "token"],
      "description": "One statement a producer owed a reader: a word plus this occasion's facts for the holes in it. `gapSentence` and `gapRoute` are the same shape with the vocabulary fixed by the field they sit in; this is the door any OTHER producer may use. It exists because prose is what a producer writes when structuring a sentence has no door: this package had built the carrier three times, each time welded to one channel, so every fourth site that held some values and owed a sentence about them wrote the sentence instead. A WORD is this shape with neither optional half — the two were separate defs of one thing until a slot had to be able to hold a whole sentence, which is what `words` pointing back here means.",
      "properties": {
        "vocabulary": {
          "description": "Which closed set the token came from. Carried rather than inferred: a token on its own does not say which set it belongs to, and two sets are free to spell a member the same way — a reader that guesses right today guesses wrong the day the second set gains that spelling. Four such collisions exist already, which is why the members below constrain the token and never the other way round.\n\nEvery set this build declares is in themis.language.VOCABULARIES under this name, and they are enumerated here because that sentence was the whole of what said so: the field was a free string, so a set name could be edited to any word at all and the reader's surface would find no table and say so about a set that never existed. A set reaching an envelope is a set the reader's surface has to hold, which makes declaring one a change to this contract — unlike a member of an open id space, which is not.",
          "$ref": "#/$defs/declaredVocabulary"
        },
        "token": {
          "type": "string",
          "description": "The member, in no language. What the reader's surface turns into a word. Free of shape on its own — which members exist is a fact about the SET, and the sets whose members are the kernel's own are enumerated in `closedSets` below."
        },
        "said": { "$ref": "#/$defs/said" },
        "words": { "$ref": "#/$defs/words" }
      },
      "allOf": [{ "$ref": "#/$defs/closedSets" }]
    },
    "declaredVocabulary": {
      "type": "string",
      "description": "Every closed set this build declares, by the name it answers to on an envelope. Held equal to `themis.language.VOCABULARIES` by a test, since that registry is filled when a vocabulary's class body runs and this is the record of it a reader's surface can hold without running anything.",
      "enum": [
        "assumption_claim", "bound_side", "bounds_note", "bridge_refusal",
        "bridge_side", "consistency_constraint", "data_contract_warning",
        "described_population", "discovery_asks", "discovery_note",
        "dose_response_routing", "e_value_undefined", "estimation_refusal",
        "extraction_refusal", "four_way_unavailable", "gap_describes",
        "gap_if_provided", "gap_routes",
        "gap_says", "instrument_route_note", "iv_required_assumption",
        "lagged_discovery_says", "late_caveat",
        "latent_lagged_discovery_says", "malformed_program",
        "markov_blanket_says", "measurement_note", "measurement_scale",
        "missing_data_shortfall", "monotonicity", "monotonicity_refutation",
        "observable_required", "orientation_asks",
        "orientation_propagation_says", "orientation_question_set_says",
        "orientation_session_says", "outcome_error_premise",
        "precision_target", "probability_statement_half",
        "proximal_criterion_failure", "proximal_data_condition",
        "proximal_role", "query_part", "query_role", "recovery_factor",
        "recovery_mechanism", "refusal_sentence",
        "selection_recovery_shortfall", "shape", "singular_matrix",
        "sutva_concern", "theta_prior_claim", "theta_refusal",
        "time_window", "unbiased_distribution", "unnamed_thing",
        "workflow_refusal"
      ]
    },
    "closedSets": {
      "description": "Which member a token may be, for each set whose members the kernel itself owns.\n\nUntil this was written the pair was two free strings, and a token could be edited to any word at all with nothing to disagree — 116 leaves across the bounds rows, the gap report and the IV block survived exactly that. The reason recorded twice for leaving them was that the verifier has no authority for these words, since they live in `themis.output.reader_words` and no verifier module may import `themis.output`. That reason was about the wrong side of the boundary: `verify` validates against this contract before any rule runs, so the contract IS the authority available here, and `reader_words._enum_at` already reads several of its sets back OUT of `query_result.schema.json`. What was missing was not a table. It was the enumeration living where the token is written.\n\nA set is listed here when its members are the kernel's own closed vocabulary. A set is NOT listed when its members are ids another layer coins — an estimator's assumption id is a free string on purpose, because an estimator adding one is not a change to the envelope — or when the set is already enumerated in the artifact's own schema, which the shared carrier must not reach back into. Both omissions are statements rather than gaps: an unlisted vocabulary is unconstrained here and says so.\n\nEach entry is conditioned on `vocabulary` rather than on the field the statement sits in, because one field carries more than one set: a ledger claim is an `assumption_claim` 248 times and a `gap_describes` 3 times in the same array.",
      "allOf": [
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "bound_side" } } },
          "then": { "properties": { "token": { "enum": ["lower", "upper"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "bounds_note" } } },
          "then": { "properties": { "token": { "enum": [
            "a_sharper_method_was_declined_for_scale",
            "one_side_tightened",
            "the_partition_has_this_many_types",
            "width_is_the_off_arm_mass"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "bridge_side" } } },
          "then": { "properties": { "token": { "enum": ["bridge_moments", "bridge_span"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "consistency_constraint" } } },
          "then": { "properties": { "token": { "enum": ["a_risk_sits_outside_its_bound"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "data_contract_warning" } } },
          "then": { "properties": { "token": { "enum": ["sample_is_below_the_advisory"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "described_population" } } },
          "then": { "properties": { "token": { "enum": ["the_strata_with_one_instrument_arm"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "dose_response_routing" } } },
          "then": { "properties": { "token": { "enum": [
            "every_effect_query_is_a_mediation",
            "no_effect_query_to_attach_to",
            "no_query_id_so_the_first_eligible_won",
            "the_query_id_matches_nothing",
            "the_query_id_names_a_mediation",
            "the_treatment_is_binary"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "e_value_undefined" } } },
          "then": { "properties": { "token": { "enum": [
            "ate_not_finite",
            "baseline_on_boundary",
            "outcome_sd_not_usable",
            "treated_rate_out_of_range"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "four_way_unavailable" } } },
          "then": { "properties": { "token": { "enum": ["the_mediator_is_continuous_under_a_nonlinear_outcome"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "instrument_route_note" } } },
          "then": { "properties": { "token": { "enum": [
            "the_polytope_rules_nothing_out",
            "the_program_did_not_solve",
            "theta_gives_a_level_no_mass"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "iv_required_assumption" } } },
          "then": { "properties": { "token": { "enum": [
            "linear_simultaneous_system",
            "monotonicity_as_declared",
            "monotonicity_or_linearity"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "late_caveat" } } },
          "then": { "properties": { "token": { "enum": [
            "late_is_not_the_ate",
            "strata_weighted_by_complier_share"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "malformed_program" } } },
          "then": { "properties": { "token": { "enum": [
            "bridge_under_determined",
            "bridges_are_each_others_mirror",
            "cause_runs_backwards",
            "const_not_in_domain",
            "covariate_not_on_both_sides",
            "discrete_channel_takes_no_covariates",
            "discrete_channel_takes_one_proxy_each",
            "forall_variable_unused",
            "free_variable_in_formula",
            "given_not_parents",
            "identify_query_cannot_transport",
            "latent_unread_by_this_query",
            "llm_prior_without_source",
            "loop_across_time_steps",
            "loop_has_one_end",
            "missingness_atom_not_in_graph",
            "no_diagram_for_this_target",
            "observation_not_ground",
            "predicate_declared_twice",
            "query_atom_not_in_graph",
            "query_atom_only_bidirected",
            "query_not_ground",
            "selection_nodes_disagree_on_target",
            "sieve_basis_too_narrow",
            "sieve_leaves_a_proxy_unused",
            "sieve_term_names_a_stranger_to_outcome_proxy",
            "sieve_term_names_a_stranger_to_treatment_proxy",
            "sum_over_not_ground",
            "treatment_bridge_not_declared",
            "treatment_bridge_unused",
            "value_not_in_domain",
            "variable_not_in_forall"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "measurement_note" } } },
          "then": { "properties": { "token": { "enum": [
            "a_field_names_a_known_noise",
            "a_threshold_cut_it_in_two"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "measurement_scale" } } },
          "then": { "properties": { "token": { "enum": ["binary", "continuous", "discrete", "nominal"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "missing_data_shortfall" } } },
          "then": { "properties": { "token": { "enum": [
            "a_product_is_blocked_by_its_factors",
            "no_recoverable_ordered_factorization",
            "the_adjusted_conditional",
            "the_covariate_marginal"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "monotonicity" } } },
          "then": { "properties": { "token": { "enum": ["non_decreasing", "non_increasing"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "monotonicity_refutation" } } },
          "then": { "properties": { "token": { "enum": ["cell_feasible_set", "response_type_polytope"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "observable_required" } } },
          "then": { "properties": { "token": { "enum": ["a_full_table_of_cells", "a_joint_distribution"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "outcome_error_premise" } } },
          "then": { "properties": { "token": { "enum": ["instruments", "mediators", "treatment_coefficient"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "precision_target" } } },
          "then": { "properties": { "token": { "enum": [
            "detect_a_binary_effect",
            "detect_a_continuous_effect",
            "detect_both_mediation_paths",
            "detect_the_effect_in_every_stratum",
            "pin_one_proportion",
            "pin_the_target_distribution",
            "trace_a_dose_response_curve"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "proximal_criterion_failure" } } },
          "then": { "properties": { "token": { "enum": [
            "covariate_is_descendant",
            "degenerate_latent",
            "latent_is_descendant",
            "latent_not_sufficient",
            "missing_node",
            "outcome_proxy_leaks_to_treatment",
            "outcome_proxy_leaks_to_treatment_proxy",
            "roles_not_distinct",
            "treatment_proxy_leaks_to_outcome"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "proximal_role" } } },
          "then": { "properties": { "token": { "enum": [
            "covariate",
            "latent",
            "outcome",
            "outcome_proxy",
            "treatment",
            "treatment_proxy"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "query_part" } } },
          "then": { "properties": { "token": { "enum": [
            "causation_query",
            "counterfactual_event",
            "longitudinal_spec",
            "proximal_role",
            "query",
            "scm_counterfactual_query"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "query_role" } } },
          "then": { "properties": { "token": { "enum": ["exposure", "instrument", "on_path_covariate", "outcome"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "recovery_factor" } } },
          "then": { "properties": { "token": { "enum": ["adjusted_conditional", "covariate_marginal"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "recovery_mechanism" } } },
          "then": { "properties": { "token": { "enum": ["from_missingness", "from_selection"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "selection_recovery_shortfall" } } },
          "then": { "properties": { "token": { "enum": [
            "no_admissible_selection_backdoor_set",
            "outcome_not_separable_from_selection",
            "treatment_or_outcome_not_in_graph"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "singular_matrix" } } },
          "then": { "properties": { "token": { "enum": [
            "bridge_instrument_moments",
            "bridge_outcome_moments",
            "design_covariance",
            "instrument_gram",
            "non_exposure_design_covariance",
            "outcome_and_mediator_fit",
            "robust_weight_matrix",
            "saturated_joint_design"
          ] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "sutva_concern" } } },
          "then": { "properties": { "token": { "enum": ["spillover_must_be_recorded", "units_must_not_coordinate"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "theta_prior_claim" } } },
          "then": { "properties": { "token": { "enum": ["a_commonsense_prior"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "time_window" } } },
          "then": { "properties": { "token": { "enum": ["baseline_and_two_follow_ups"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "unbiased_distribution" } } },
          "then": { "properties": { "token": { "enum": ["the_weights", "unbiased"] } } }
        },
        {
          "if": { "required": ["vocabulary"], "properties": { "vocabulary": { "const": "unnamed_thing" } } },
          "then": { "properties": { "token": { "enum": [
            "intervention",
            "outcome",
            "population",
            "source_population",
            "target_population"
          ] } } }
        }
      ]
    }
  }
}

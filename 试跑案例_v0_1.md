# v0.1.0 试跑案例

这份清单的目的不是再做功能开发，而是用 `v0.1.0` 的真实能力去回答一个更实际的问题：

- 下一个版本最先该补的是 **识别能力**
- 还是 **数值/置信/调查能力**
- 还是 **表达与解释的可用性**

建议先跑两个案例，再决定 `v0.2` 的第一条 slice。

---

## 案例 1：经典成功路径

文件：

- [`tests/test_e2e/fixtures/numeric_backdoor.json`](tests\test_e2e\fixtures\numeric_backdoor.json)

场景：

- `stress -> smokes`
- `stress -> cancer`
- `smokes -> cancer`

问题：

- `effect(cancer=true | do(smokes=false))`

这个案例验证的是当前 `v0.1.0` 的主成功链是否顺手：

1. 结构是否容易表达
2. 后门识别是否符合直觉
3. 公式是否可解释
4. 数值结果是否直接可得

期望：

- 识别出调整变量 `stress`
- 构造后门公式
- 返回 `numerically_solved`
- 数值结果为 `0.18`

你在试跑时重点看这 4 个问题：

1. 这个 JSON 写法是否已经“够像一种语言”
2. 公式和解释是否能让你快速确认它是对的
3. 你是否很自然地知道需要补哪些 `probability` 条目
4. 如果把这个系统拿给别人，别人能不能顺着结果继续工作

---

## 案例 2：参数缺口边界路径

文件：

- [`tests/test_e2e/fixtures/numeric_backdoor_missing_parameter.json`](tests\test_e2e\fixtures\numeric_backdoor_missing_parameter.json)

场景：

- 图结构与案例 1 完全相同
- 但故意缺一条关键参数：
  - 缺少 `P(cancer=true | smokes=false, stress=false)`

问题：

- `effect(cancer=true | do(smokes=false))`

这个案例不是为了让系统成功，而是为了检验：

1. `needs_investigation` 是否足够诚实
2. 缺失参数是否报告得足够精确
3. 调查请求是否已经对真实工作流有帮助

期望：

- 返回 `needs_investigation`
- `missing_information` 里指向精确缺失项
- `investigation_requests` 能直接转成下一步动作

你在试跑时重点看这 4 个问题：

1. 缺口报告是否一眼就能看懂
2. 缺失项命名是否足够精确，能直接回填
3. 你是否会想要“数据来源 / 置信传播 / 参数推荐”这些更强能力
4. 这个体验是否让你更倾向先做 `confidence` / 数据接口，而不是先做 `ID`

---

## 如何用这两个案例决定 v0.2

### 如果案例 1 已经很顺，但案例 2 明显卡住

优先考虑：

- `confidence` 正式语义
- 数据入口 / 参数来源
- 更好的调查推进

也就是先走“数值与证据层”。

### 如果案例 1 的结构/公式已经开始显得不够

优先考虑：

- `ID` / `ananke`
- 更强识别
- 更完整公式 AST

也就是先走“识别能力层”。

### 如果两个案例都能跑，但写起来和读起来都偏重

优先考虑：

- 更好的前端表达
- 示例库
- 解释器和错误消息

也就是先走“可用性层”。

---

## 可选第三案例

如果你想再加一个专门压结构识别的案例，可以直接跑：

- [`tests/test_e2e/fixtures/identify_two_var.json`](tests\test_e2e\fixtures\identify_two_var.json)

它适合回答：

- 多变量调整的公式是否仍然可读
- 识别结果在规模稍大时是否还清楚

但如果只选两个，建议先跑前面两个。

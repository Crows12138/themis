# 因果推理内核设计 v0.1

这份文档不是教材笔记，而是当前项目的第一版设计规范。

它回答四件事：

1. 语言里允许谈什么
2. 系统里允许问什么
3. 每类问题什么时候算有答案
4. 因果与相关的核心判断规则是什么

---

## 1. 设计总纲

当前内核采用下面这组立场：

- 结构层是确定的、封闭的、可枚举的
- 数值层是不确定的，允许置信度、缺失数据和调查需求
- 语言本体是关系化表达，不把图当作唯一真相
- 图是计算层和可视化层的标准投影
- 因果性由有向因果链定义
- 实际相关性由当前条件下的开放路径定义
- 新证据主要通过概率更新修正结论，而不是靠“默认真后撤回”的非单调逻辑
- 结构推理是单调的，不支持默认规则撤回
- v0.1 只做静态推理，不处理时间、动作序列、规划

一句话总结：

```text
以关系表达为本体，以图为计算投影，以概率为数值层，以因果链和开放路径为核心语义。
```

---

## 2. 语言对象

### 2.1 允许进入语言的对象

v0.1 允许下面这些对象进入语言：

- 个体，例如 `alice`、`bob`
- 谓词，例如 `smokes(x)`、`cancer(x)`
- 关系，例如 `parent(x,y)`、`causes(x,y)`
- 变量实例，例如 `smokes(alice)`
- 因果依赖，例如 `cause(smokes(x), tar(x))`
- 条件集，例如 `given(C)`
- 观测，例如 `observe(fever(alice)=true)`
- 干预，例如 `do(smokes(alice)=false)`
- 概率表达，例如 `P(cancer(alice) | smokes(alice))`
- 查询表达式

### 2.2 对象域

v0.1 默认使用有限、已实例化对象域。

也就是：

- 当前有哪些对象，就只对这些对象推理
- 默认不在无限对象集合上直接运算
- 默认不自动假设新的未实例化对象进入主推理过程

如果将来需要表达“可能存在但当前未实例化的对象”，应通过单独机制处理，而不是混入主推理内核。

### 2.3 当前不支持

v0.1 当前明确不支持：

- 无限对象域上的直接推理
- 高阶量词
- 时间索引
- 动作序列
- 规划
- 反事实

这里“高阶量词”指对性质、关系、规则本身量化，而不是只对对象量化。

---

## 3. 表达层

### 3.1 表达能力

v0.1 的表达目标是：

- 至少关系级
- 支持受限一阶表达

也就是：

- 可以写对象、谓词、关系
- 可以写 `forall`
- 可以把一般规则应用到多个对象实例

例如：

```text
forall x:
  cause(smokes(x), tar(x))
  cause(tar(x), cancer(x))
```

### 3.2 执行方式

虽然语言层支持受限一阶表达，但执行层默认采用实例化后的图结构。

例如：

```text
smokes(alice) -> tar(alice) -> cancer(alice)
smokes(bob) -> tar(bob) -> cancer(bob)
```

因此当前架构是：

- 语言层：关系化、受限一阶
- 执行层：ground 后的图

### 3.3 图的角色

图不是语言本体，但图是标准工作表示。

图的作用有两个：

- 计算：路径搜索、开放/封闭判断、结构检查
- 可视化：让依赖关系可读、可审查

因此本项目采取的立场是：

- 逻辑表达和图投影应尽量一一对应
- 对复杂结构，允许后续扩展为模板图、关系节点图或更广义图表示

---

## 4. 世界假设

### 4.1 结构层：封闭世界

图结构层采用封闭世界假设。

也就是：

- 图里没画的边，默认不存在
- 当前结构输入就是当前推理依据

这样做的原因：

- 结构稀疏
- 易于枚举
- 易于计算
- 便于穷举路径和判断依赖

### 4.2 数值层：允许未知

数值层不采用“缺失即假”，而采用：

- 可知
- 未知
- 待调查

也就是：

- 缺失数据不等于事件为假
- 缺失数据会降低数值可解性
- 系统应显式返回缺失信息和调查需求

### 4.3 这不是矛盾

本项目的世界假设不是简单二选一，而是分层：

- 结构层封闭
- 数值层允许未知

这意味着：

- 哪些边存在，由结构输入决定
- 边上参数是多少，是否可靠，由数据和置信度决定

---

## 5. 更新机制

### 5.1 当前选择

v0.1 不把“默认真后撤回”的非单调逻辑作为核心。

系统的主要修正机制是：

- 概率更新
- 条件更新
- 参数更新

### 5.2 含义

例如：

```text
P(Fly(x) | Bird(x)) = 0.99
P(Fly(x) | Penguin(x)) = 0.01
Penguin(x) -> Bird(x)
```

这里不是：

- 先硬推出 `Fly(Tweety)`
- 再把它撤回

而是：

- 在条件变多后，分布发生更新
- 后验概率坍缩到更具体条件下的值

### 5.3 后续扩展

如果未来需要支持：

- 规则优先级
- 法律例外
- 默认适用、后续覆盖

也优先考虑：

- 概率 + 判断标准
- 证据更新
- 规则权重

而不是先上独立的非单调逻辑核心。

---

## 6. 查询类型

凡是属于语言定义范围的查询都可以提交。

v0.1 主要支持下面几类查询。

### 6.1 结构查询

结构查询只问结构本身。

例如：

- `DoesCause(X, Y)`
- `FindDirectedPaths(X, Y)`
- `FindOpenPaths(X, Y | C)`
- `IsCollider(Z, path)`

### 6.2 相关查询

相关查询问实际相关性及其来源。

例如：

- `Assoc(X, Y | C)`
- `ExplainAssoc(X, Y | C)`

### 6.3 干预查询

干预查询问主动改变变量后的影响。

例如：

- `Effect(Y, do(X=x), C)`
- `WillChange(Y, do(X=x), C)`

### 6.4 识别查询

识别查询问某个干预量能否用观测量表达。

例如：

- `Identifiable(P(Y | do(X), C))`
- `Identify(P(Y | do(X), C))`

### 6.5 数值查询

数值查询问具体概率或数值结果。

例如：

- `P(Y | X)`
- `P(Y | X, C)`
- `P(Y | do(X), C)`

### 6.6 解释查询

解释查询问结论为什么成立。

例如：

- `ExplainCause(X, Y)`
- `ExplainAssoc(X, Y | C)`
- `ExplainEffect(Y, do(X=x), C)`

---

## 7. 查询输出类型

系统对语言内查询的输出分三类。

### 7.1 结构答案

适用于只依赖结构的查询。

例如：

- 是否存在因果链
- 是否存在开放路径
- 某查询在结构上是否可识别

### 7.2 数值答案 + 置信度

适用于结构和参数都足够的查询。

例如：

- `P(Y | X)`
- `P(Y | do(X), C)`

输出应包含：

- 数值结果
- 复合置信度，必要时附带可信区间
- 主要数据来源

### 7.3 公式/结构答案 + 缺失信息 + 调查需求

适用于结构已足够，但数值数据不够的查询。

系统应返回：

- 当前可得的结构结论
- 如能写出则返回公式
- 明确指出缺什么参数或数据
- 给出调查方向

例如：

- 需要观测哪些变量
- 需要补哪类样本
- 哪条边上的参数置信度过低

---

## 8. 什么叫“可解”

本系统把“可解”拆成三层。

### 8.1 结构可解

如果只靠结构信息就能回答，则称为结构可解。

例如：

- `DoesCause(X, Y)`
- `Assoc(X, Y | C)`

### 8.2 数值可解

如果结构已定，且参数/数据足够给出数值，则称为数值可解。

例如：

- `P(Y | X)`
- `P(Y | do(X), C)`

### 8.3 调查可推进

如果结构已足够，但数据不完整，则称为调查可推进。

这时系统不应简单返回失败，而应返回：

- 结构上已经知道什么
- 数值上缺什么
- 下一步调查什么

### 8.4 语言外问题

如果一个问题不属于当前语言定义范围，则直接标记为语言外。

例如：

- 涉及时间的多步动作规划
- 反事实推理
- 高阶规则元推理

---

## 9. 因果与相关的核心区分

必须始终分清下面两种查询：

- `P(Y | X)`：观察到 `X` 后，`Y` 的概率
- `P(Y | do(X), C)`：主动把 `X` 改掉，并在条件集 `C` 下看 `Y` 的概率

结论：

- 观察不等于干预
- 相关不等于因果
- 能从结果反推原因，不等于结果反向造成原因

例子：

```text
Rain -> Umbrella
```

- 因果方向：下雨导致打伞
- 观察相关：看到很多人打伞，也会更相信在下雨
- 但 `do(Umbrella = true)` 不会导致下雨

---

## 10. 因果性的定义规则

### 10.1 基本定义

若要判断 `X` 是否对 `Y` 有因果影响，先看：

- 是否存在从 `X` 出发、顺着箭头能够到达 `Y` 的有向路径

若存在：

- `X` 对 `Y` 具有结构上的因果影响

若不存在：

- `X` 对 `Y` 不具有结构上的因果影响

### 10.2 因果性只看有向因果链

例如：

```text
X -> M -> Y
```

则：

- `X` 对 `Y` 有因果作用

而：

```text
X <- Z -> Y
```

则：

- `X` 和 `Y` 可能相关
- 但不意味着 `X` 导致 `Y`

### 10.3 诊断推理不等于反向因果

例如：

```text
Flu -> Fever
```

可以做：

```text
P(Flu | Fever)
```

这只是诊断推理，不表示：

```text
Fever -> Flu
```

---

## 11. 实际相关性的定义规则

### 11.1 基本定义

实际相关性不由“有没有因果链”单独决定，而由：

- 当前条件下是否存在至少一条开放路径

决定。

可以压成一句话：

```text
实际相关性 = 当前条件下是否存在开放路径
```

### 11.2 三种基础结构

所有复杂路径都由三种基础局部结构组成。

#### 链

```text
X -> M -> Y
```

性质：

- 默认开
- 控制中间点后关

#### 叉

```text
X <- Z -> Y
```

性质：

- 默认开
- 控制共同原因后关

#### 碰撞点

```text
X -> C <- Y
```

性质：

- 默认关
- 控制碰撞点后开

### 11.3 判断开放路径的总规则

- 普通中间点被控制，会堵路
- 碰撞点被控制，会开路

这里“控制”统一表示：

- 已知它的值
- 条件化在它上面
- 只看满足某条件的样本
- 在模型里把它调整掉

### 11.4 相关性来源

实际相关性可以来自三类路径：

- 有向因果链
- 共同原因路径
- 因条件化而打开的碰撞路径

因此：

- 后门只是相关来源的一类
- 不是相关性的全部

---

## 12. 识别与控制

### 12.1 控制变量的目标

控制变量不是为了“变量越多越好”，而是为了：

- 堵住错误路径
- 保留真正需要的因果路径

### 12.2 什么时候看后门

后门不是用来判断“有没有因果链”的。

后门主要用于：

- 因果识别
- 解释混杂
- 选择控制集

### 12.3 哪些变量通常该控制

通常应该控制：

- 共同原因
- 会把伪相关混入主问题的混杂变量

### 12.4 哪些变量通常不要乱控制

通常不要乱控制：

- 碰撞点
- 作为主因果机制一部分的中介变量

### 12.5 中介变量

如果目标是：

- 总效应：通常不控制中介
- 直接效应：可以控制中介

---

## 13. v0.1 的完整工作流

### 13.1 输入

系统接收：

- 对象实例
- 关系规则
- 因果结构
- 观测数据
- 参数或样本
- 查询表达式

### 13.2 结构编译

系统把关系表达编译为当前对象域上的工作图。

### 13.3 结构推理

系统回答：

- 是否存在有向因果链
- 是否存在开放路径
- 哪些路径导致相关
- 哪些变量适合控制

### 13.4 数值推理

若参数充分，则进一步回答：

- 观测概率
- 干预概率
- 数值估计
- 置信度

### 13.5 调查触发

若结构已定但数据不足，则返回：

- 缺失参数
- 缺失观测
- 建议调查项

---

## 14. 当前边界

v0.1 明确聚焦于：

- 静态
- 结构清晰
- 对象域有限
- 查询语义明确
- 结构推理优先
- 数值推理可逐步接入

v0.1 暂不承诺：

- 时间推理
- 动作序列
- 规划
- 反事实
- 无限对象域
- 高阶元推理

---

## 15. 语法草案 v0.1

本节给出第一版外部写法和内部规范方向。

当前原则：

- 外部语法用声明式 DSL
- 内部规范使用标准 AST
- 图是由 AST 编译出来的工作表示

### 15.1 语法风格

v0.1 采用函数式、ASCII 友好的声明语法。

原因：

- 易解析
- 易转 AST
- 易映射到 JSON
- 易与图结构一一对应

### 15.2 基本书写约定

- 常量名使用小写标识符，例如 `alice`
- 谓词名使用小写标识符，例如 `smokes`
- 模式变量使用大写标识符，例如 `X`
- 每条声明单独成句
- 一般规则可含 `forall`

### 15.3 核心声明类型

#### 对象声明

```text
object(alice).
object(bob).
```

#### 因果规则声明

```text
cause(smokes(X), tar(X)).
cause(tar(X), cancer(X)).
```

#### 概率声明

```text
prob(cancer(X) | tar(X)) = 0.20 [confidence=0.82].
prob(fly(X) | bird(X)) = 0.99 [confidence=0.91].
prob(fly(X) | penguin(X)) = 0.01 [confidence=0.97].
```

这里的 `prob(...)` 在 v0.1 中表示模型世界的条件分布项，不表示经验样本频率。

因此：

- `given` 应语义上对应目标变量的结构父集，或其子集
- 若以后需要表达观测统计，应单独增加经验概率语句，而不是复用 `prob`

#### 观测声明

```text
observe(smokes(alice)=true) [confidence=1.00, source=clinical_record].
observe(fever(alice)=true) [confidence=0.88].
```

v0.1 不设独立 `fact` 语义。

也就是：

- 世界本体由结构声明和分布声明给出
- 所谓“事实输入”应写成高置信观测
- `confidence=1` 表示当前轮推理中的硬证据，不表示形而上的绝对真理

#### 查询声明

```text
query(cause(smokes(alice), cancer(alice))).
query(assoc(smokes(alice), cancer(alice) | [])).
query(effect(cancer(alice) | do(smokes(alice)=false), [])).
query(prob(cancer(alice) | tar(alice))).
query(identify(prob(cancer(alice) | do(smokes(alice)=false), []))).
```

### 15.4 一般规则与实例

语言层允许写一般规则：

```text
forall X:
  cause(smokes(X), tar(X)).
```

执行层会在当前有限对象域上实例化，例如：

```text
cause(smokes(alice), tar(alice)).
cause(smokes(bob), tar(bob)).
```

### 15.5 条件集写法

相关查询中的条件集使用列表表示：

```text
query(assoc(X, Y | [Z1, Z2])).
```

空条件集写成：

```text
query(assoc(X, Y | [])).
```

### 15.6 内部 AST 节点建议

内部最小节点类型建议至少包括：

- `Object`
- `Cause`
- `Prob`
- `Observe`
- `ConditionSet`
- `Do`
- `FormulaExpression`
- `Query`

其中：

- `Cause(A, B)` 表示结构因果依赖
- `Prob(Target, Given, Value, Confidence)` 表示模型条件分布项
- `Do(Target, Value)` 表示干预
- `FormulaExpression` 表示可继续计算的结构化公式
- `Query(Kind, Payload)` 表示查询入口

### 15.7 语法边界

v0.1 的 DSL 先不支持：

- 时间下标语法
- 反事实索引
- 高阶规则量化
- 无限对象域生成语法

这些如果以后加入，建议通过新节点类型扩展，而不是破坏当前核心语法。

### 15.8 配套文件

本节对应的配套文件如下：

- AST schema: [kernel_ast.schema.json](kernel_ast.schema.json)
- 查询结果 schema: [query_result.schema.json](query_result.schema.json)
- 最小示例: [minimal_example_v0_1.json](minimal_example_v0_1.json)
- 理论框架: [理论框架_v0_1.md](理论框架_v0_1.md)

---

## 16. 执行语义草案 v0.1

本节规定系统如何把声明式表达转成可执行推理过程。

### 16.1 总流程

对任一输入，系统按下面顺序处理：

1. 解析为 AST
2. 检查是否超出语言边界
3. 在当前对象域上做实例化
4. 编译为工作图和参数表
5. 按查询类型分派到对应求解器
6. 产出结构答案、数值答案或调查需求

### 16.2 解析阶段

解析器负责把文本转为 AST，并检查：

- 标识符是否合法
- 变量是否绑定
- 查询类型是否合法
- 是否使用了当前版本未支持的语法

### 16.3 实例化阶段

对于受限一阶规则，系统在当前对象域上做有限实例化。

例如：

```text
forall X:
  cause(smokes(X), tar(X)).
```

若对象域为 `{alice, bob}`，则展开为：

```text
cause(smokes(alice), tar(alice)).
cause(smokes(bob), tar(bob)).
```

### 16.4 编译阶段

AST 编译为两类工作对象：

- 结构图
- 参数表

编译规则如下：

- `Cause(A, B)` 生成有向边 `A -> B`
- `Observe(A=v)` 把观测值、置信度和来源写入证据集
- `Do(A=v)` 不直接改写原图，而在查询上下文中生成干预图
- `Prob(...)` 写入模型条件分布参数表

### 16.5 干预语义

`do(A=v)` 的执行语义是：

- 在查询上下文里切断所有指向 `A` 的入边
- 把 `A` 固定为指定值
- 在此修改后的图上计算查询结果

因此：

- 原图不被永久修改
- 干预图只在当前查询生命周期内存在

### 16.6 结构查询语义

结构查询只读取图结构，不依赖数值参数。

例如：

- `cause(X, Y)`：检查是否存在 `X -> ... -> Y` 的有向路径
- `assoc(X, Y | C)`：检查在条件集 `C` 下是否存在开放路径
- `identify(...)`：检查目标干预查询在当前结构语义下是否可表达或可归约

### 16.7 相关查询语义

相关查询按开放路径规则处理：

- 枚举 `X` 到 `Y` 的候选路径
- 对每条路径检查中间点类型
- 链和叉节点若被条件化则堵路
- 碰撞点若被条件化则开路
- 只要存在至少一条开放路径，即返回结构上相关

### 16.8 因果查询语义

因果查询按有向链处理：

- 只考虑从 `X` 出发沿箭头到达 `Y` 的路径
- 若存在至少一条有向路径，则返回结构上存在因果影响
- 若不存在，则返回结构上不存在因果影响

### 16.9 数值查询语义

若一个查询需要数值结果，则系统进一步检查：

- 相关参数是否存在
- 参数置信度是否达标
- 样本是否足够

若充分，则返回：

- 数值结果
- 置信度或可信区间
- 使用过的参数来源

### 16.10 调查语义

若结构已定，但数值信息不足，则系统进入调查输出模式。

系统应返回：

- 当前已知的结构结论
- 当前还缺哪些参数
- 每个缺失项的优先级
- 推荐补充的观测或实验

典型调查项包括：

- 缺少某条件概率项
- 某边参数置信度过低
- 某关键变量缺观测样本
- 某中介变量未被观测

### 16.11 标准输出结构建议

所有查询结果建议统一输出为：

- `status`
- `query_kind`
- `structural_result`
- `numeric_result`
- `confidence`
- `formula`
- `missing_information`
- `investigation_requests`
- `explanation`

其中：

- `status` 可取 `structurally_solved`、`numerically_solved`、`needs_investigation`、`outside_language`
- `formula` 应为结构化公式 AST，而不是仅供显示的字符串
- `confidence` 表示当前结果的复合可信度分数，不等于统计置信区间

### 16.12 图与逻辑的一致性要求

执行层必须满足：

- 每个关系实例都能映射到明确节点或节点关系
- 每条因果声明都能映射到明确有向边
- 每个结构查询都能回溯到原始逻辑表达
- 每个图上结论都能解释回原始声明

这条要求的目的，是保证：

- 图不是黑箱
- 逻辑表达与图计算完全对应

---

## 17. 最终压缩版

如果只保留最短规则，可以记成下面这组：

- 语言本体是关系表达，图是计算投影
- 结构层封闭，数值层允许未知和调查
- 因果性看有向因果链
- 实际相关性看开放路径
- 新证据主要通过概率更新修正结论
- 语言内查询都可提交
- 输出分为：结构答案、数值答案、调查需求
- 控制变量的目标是堵错路，不是乱加条件

---

## 18. 一句总纲

```text
在一个关系化、有限对象域、静态的因果系统中，用图做计算投影，用有向链定义因果，用开放路径定义相关，用概率和调查机制处理数值不确定性。
```
